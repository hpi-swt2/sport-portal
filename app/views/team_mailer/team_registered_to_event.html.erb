<h1><%= t('mailer.hello', firstname: @user.first_name) %></h1>
<p><%= t('mailer.team_mailer.team_registered_to_event.registered_to_event_info_html', team_name: @team.name, event_link: link_to(@event.name, event_url(@event))) %></p>
<p><%= t('mailer.team_mailer.team_registered_to_event.have_fun') %></p>
<p><%= t('mailer.closing', appname: t('appname')) %></p>