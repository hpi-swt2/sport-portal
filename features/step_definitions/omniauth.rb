Given(/^an unlinked OpenID account$/) do
  pending #TODO: mock omniauth
end

Given(/^the account is used for authentication$/) do
  pending #TODO: mock authentication callback
end


When(/^the account is used to sign in$/) do
  pending #TODO: mock authentication
  #NOTE: this will just open the sign in page, since the
end
